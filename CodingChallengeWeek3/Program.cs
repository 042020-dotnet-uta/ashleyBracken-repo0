using System;

namespace CodingChallengeWeek3
{
    class Program
    {
        static void Main(string[] args)
        {
            RunApplication.RunApp();
        }

        /// Notes: On considering how to achieve task below
        /// Check if Input Number is Even 
        ///     Divide input number by 2 if == 0 then it is even
        /// For the multiplication table use a nested For Loop
        /// For the Shuffle use 2 lists to store the input and for each element in list assign 
      
        
    }
}
